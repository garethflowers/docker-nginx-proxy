language: bash

addons:
  apt:
    update: true

services:
  - docker

install:
  - docker build --tag nginx-proxy .
  - docker run --detach --publish 80:80 --name nginx-proxy nginx-proxy
  - sleep 10

script:
  - docker exec nginx-proxy nginx -t
